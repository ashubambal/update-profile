name: ⏰ Update README Automatically

on:
  schedule:
    - cron: '0 */12 * * *' # Runs every 12 hours
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Run readme updater
        run: |
          echo "📝 Updating README dynamically..."
          # You can use curl or node/python scripts here to update README.md
          # Example: curl to fetch top languages, update using sed
          # Replace this block with your own logic
          
          python update_readme.py

      - name: Commit & Push Changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md
          git commit -m "🔄 Auto-updated README" || echo "No changes to commit"
          git push
